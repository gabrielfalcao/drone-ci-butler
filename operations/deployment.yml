---
apiVersion: v1
automountServiceAccountToken: false
kind: ServiceAccount
metadata:
  name: drone-ci-monitor-user
secrets:
  - name: postgresql-password

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "drone-ci-monitor-io.fullname" . }}-web
spec:
  serviceAccountName: drone-ci-monitor-user
  minReadySeconds: 5
  replicas: 1
  revisionHistoryLimit: 10
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "drone-ci-monitor-io.name" . }}-web
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "drone-ci-monitor-io.name" . }}-web
        app.kubernetes.io/instance: {{ .Release.Name }}
    spec:
      affinity:
        # kubernetes.io/hostname=pool-cognodes-361bk
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: kubernetes.io/hostname
                operator: In
                values:
                - pool-cognodes-361bk

          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 1
            preference:
              matchExpressions:
              - key: kubernetes.io/hostname
                operator: In
                values:
                - pool-cognodes-361bk

      initContainers:
      - image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        name: check-installation
        command: ['/venv/bin/drone-ci-monitor-io', 'check']
        envFrom:
          - configMapRef:
              name: {{ include "drone-ci-monitor-io.fullname" . }}-env

      - image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        name: check-db-connection
        command: ['/venv/bin/drone-ci-monitor-io', 'check-db']
        envFrom:
          - configMapRef:
              name: {{ include "drone-ci-monitor-io.fullname" . }}-env

      - image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        name: run-database-migrations
        command: ['/venv/bin/drone-ci-monitor-io', 'migrate-db']
        envFrom:
          - configMapRef:
              name: {{ include "drone-ci-monitor-io.fullname" . }}-env
        env:
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: {{ template "drone-ci-monitor-io.fullname" . }}-secrets
              key: postgresql-password

        env:
        - name: DOCKER_IMAGE
          value: {{ .Values.image.tag }}

      - image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        name: purge-sessions
        command: ['/venv/bin/drone-ci-monitor-io', 'purge-sessions']
        envFrom:
          - configMapRef:
              name: {{ include "drone-ci-monitor-io.fullname" . }}-env

      volumes:
        - name: drone-ci-monitor-uploads-volume
          persistentVolumeClaim:
            claimName: pvc-drone-ci-monitor-io

      containers:
      - image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        name: {{ .Chart.Name }}-web-application-server
        command: ['/venv/bin/drone-ci-monitor-io', 'web']
        livenessProbe:
          httpGet:
            path: /health
            port: api
          initialDelaySeconds: 60
          periodSeconds: 600
        readinessProbe:
          httpGet:
            path: /health
            port: api
          initialDelaySeconds: 1

        env:
          - name: UPLOAD_FOLDER
            value: /drone-ci-monitor.io/file-uploads
          # - name: OAUTH2_CLIENT_SECRET
          #   valueFrom:
          #     secretKeyRef:
          #       name: {{ template "drone-ci-monitor-io.fullname" . }}-secrets
          #       key: oauth2-client-secret

        envFrom:
          - configMapRef:
              name: {{ include "drone-ci-monitor-io.fullname" . }}-env

        ports:
        - containerPort: 5000
          name: api

        volumeMounts:
          - name: drone-ci-monitor-uploads-volume
            mountPath: /drone-ci-monitor.io
